# QA log

## Phase 0 — Foundations

**Built:** Next 15.5 App Router scaffold (TypeScript strict, Tailwind v4, next-intl v4 with `/` and `/ar`, GSAP and Lenis installed), the full token set from docs/03 §3 in `src/app/globals.css` with the Tailwind palette removed so no other colour can exist, the type scale as `.t-*` classes with `clamp()` between 390 and 1440, the glass and liquid recipes verbatim, self-hosted Bodoni Moda / Manrope / Amiri / IBM Plex Sans Arabic through `next/font/local`, `src/lib/content.ts` (single content module, localises cars, rows, preview, categories), `src/lib/images.ts` (manifest lookup, blur placeholders, cut-out alpha bounds), `scripts/blur-placeholders.mjs` (151 placeholders + 22 alpha bounds), the emblem redrawn as three strokes and the wordmark traced from the masters (`public/brand`, `src/components/brand`), the base UI (`Section`, `MediaBg`, `Eyebrow`, `Button` ×5, `GlassCard`, `LiquidBadge`, `EnvCaption`, `SpecChip`, `GiantWord`, `Icon`), the static `Scene` with both compositions and the RTL mirror, the dev-only `/styleguide` and `/ar/styleguide`, favicon from the emblem, QA scripts (`scripts/shoot.mjs`, `peek.mjs`, `measure.mjs`, `overflow.mjs`).

**Left:** everything from Phase 1 on (shell, pages, motion). Home currently renders only the wordmark and tagline. Media-scoped preload for the desktop hero image (Phase 7). Apple touch icon and OG image (Phase 7).

**Decisions and disagreements with the spec (docs/02 wins where it is explicit; these are where it is silent or self-contradictory):**
- Bodoni Moda has no 300 weight (the variable face is 400–900), so the Statement style is 400 italic.
- Fonts are self-hosted subsets via `next/font/local`, as §2.2 asks ("serve self-hosted WOFF2"); the build-time Google Fonts fetch in `next/font/google` hit connection resets on this network. Arabic faces are the `arabic` subsets only and the Arabic stacks fall through to Bodoni / Manrope, so Western numerals and Latin names inside Arabic copy set in the Latin faces (§1.3, §2.9).
- Giant word on mobile: Bodoni capitals are ~0.62 em wide, so an 8-letter word at 22vw is 429 px on a 390 px screen. `GiantWord` scales Latin words longer than seven characters to the viewport minus gutters (CULLINAN → 69 px at 390). Multi-word texts (MAY BACH) wrap to two lines at the spec size.
- Scene word position on desktop: §3.9's rule ("baseline at 58% of the car's height") puts the whole word behind the car body; the sketch and the sentence beside it ("top of word above the roofline, bottom hidden behind the car") are followed instead. The word box sits 30% of its height below the measured roofline (from the cut-out's alpha bounds) and ends inside the viewport with a 2vw gutter rather than bleeding off with the car.
- Spec values are wrapped in an LTR isolate so "563 hp" keeps its order in Arabic layout (§12 of docs/03 for Latin names, applied to numerals too).
- next-intl ships no message bundle to the client; all copy flows through `content.ts` on the server.
- `MediaBg` gives `priority` to the mobile image and `fetchPriority="high"` to the desktop one; a media-scoped preload for the desktop hero is deferred to Phase 7 so mobile never downloads both.
- The eyebrow hairline is `currentColor` at 40% (silver) rather than the 10% white hairline, which is invisible at 24 px next to 12 px type.

**Screenshots:** `qa/phase-0/styleguide-{390,1440}.png`, `ar-styleguide-{390,1440}.png`, `home-*.png`, `ar-*.png`, plus `detail-*.png` viewport crops of the Scene, glass, type and brand blocks at both widths.

## Phase 1 — Global shell

**Built:** Nav (transparent over the hero, floating glass pill after 80 px with carbon at 82% behind the blur, desktop links with the 4 px active dot, language toggle, Reserve pill; mobile two-hairline ☰ opening the full-screen obsidian overlay on the BR-03 texture with Display L links staggered 80 ms, toggle and WhatsApp at the bottom, focus trap, Escape, scroll lock), Footer (§3.15: 20% platinum hairline, 40 px wordmark, tagline, four link columns plus address/hours/phone, thin social glyphs, toggle, licence, copyright, texture at 40%), StickyBar (mobile only, appears after the hero, Enquire 60% + Concierge 40% on the thumb side, 52 px buttons, hidden while the menu is open, per-page override store for car detail pages), Grain (5% SVG turbulence tile, overlay), SmoothScroll (Lenis lerp .08, off under reduced motion), Preloader (emblem draws 700 ms with staggered strokes, wordmark fades 400 ms, curtain lifts 800 ms, 1.65 s total, once per session, no flash on repeat visits thanks to an inline script), PageTransition (curtain wipes up 800 ms ease-inout, emblem draws 600 ms, lifts 600 ms once the route has committed, safety lift at 4 s, skipped under reduced motion), PageHero (two-layer word-over-photo hero for COLLECTION, RIYADH, BEYOND, MAJLIS), the 404 on UT-404, hero-only Collection / Showroom / Services / Visit routes with titles, favicon from the emblem. Lighthouse mobile on `/collection` and `/ar/collection`: accessibility 100, best practices 100, SEO 100 (`qa/lighthouse/phase-1-*.json`).

**Left:** Home (Phase 2), page bodies (Phases 3 to 5), the optional desktop cursor (§3.17).

**Decisions:** Privacy and Terms are rendered as text: the copy names them but the site map has no pages (`qa/QUESTIONS.md`). The footer's four columns hold one link each because the copy has no sub-links. The Reserve pill is hidden under 768 px, where the sticky bar carries Enquire. Visit and Services heroes place the copy at the bottom over the dark ground because their photographs have the subject mid-frame; Collection keeps the copy under the word as sketched; Showroom keeps RIYADH just under the eyebrow so the gradient dissolves it between sky and building. The idle transition curtain and the hidden sticky bar are `visibility: hidden` so they never enter the tab order.

**Screenshots:** `qa/phase-1/{home,collection,showroom,services,visit,nothing-here}-{390,1440}.png` and the `ar-*` set, plus `detail-*` for the nav pill, open menu, footer and preloader in both locales.

## Phase 2 — Home

**Built:** all ten sections. H1 Scene (HM-HERO-BG + CUL-CUT-A/-B, MAY BACH, copy start-side, the now-showing glass card with a 72 px detail of the car, scroll cue; entrance bg 1.06→1 / word mask / car rise / copy stagger / card last, started as the preloader lifts; scroll parallax bg 0.85× · word 0.92× with −40 px drift and opacity to .35; pointer parallax ±10 / ∓6 px on hover devices; cut-out failure swaps in HM-HERO-STILL and hides the word). H2 Statement (word-by-word 120 ms, support line 400 ms later, background 0.9×). H3 PhotographRow (RRA, start) → CardPair (SCL + YUK) → PhotographRow (C8, end) with the whole-row stretched link, badges, accent captions, glass spec card, 40 px photo pan. H4 Promise: headline staggered over its lines, three GlassCards; desktop pins 1.5 viewports while the cards slide into a stack and the photo pans 60 px; mobile staggered reveals. H5 Marques on carbon with the 3% emblem watermark, 40 s marquee that pauses on hover and scales the centre tile 1.15. H6 After dark (SH-05, scroll-scrubbed 1.08→1). H7 Featured Scene, the centred exhibit (CGT-BG + CGT-CUT-B, AZURE). H8 Private viewing (HM-VIEW). H9 Visit strip (HM-MAP, glass panel end-side). Reveal primitives (24 px / 800 ms / 80 ms stagger; masked line reveal over 1200 ms measured per rendered line, re-measured after fonts load; 200 ms opacity under reduced motion; nothing hidden without JS). GSAP + ScrollTrigger load lazily on first use and sync with Lenis. Home first-load JS 118 kB.

**Left:** Collection page (Phase 3), car detail (Phase 4), Showroom / Services / Visit bodies (Phase 5). Official marque SVGs: Wikimedia is unreachable from this network and the kickoff lists the logos as client-supplied, so tiles show the marque names in Manrope 500 uppercase at 70% until `public/logos/<slug>.svg` files exist (they are picked up automatically, rendered white). No live map in H9 (needs a Mapbox/Google key): the HM-MAP photograph carries the section.

**Decisions:** the mobile Scene copy is pulled up over the cut-out's transparent floor margin (measured from the alpha bounds) so the hero fits the fold; the scroll cue shows on desktop only. Landscape side-profile cut-outs (CGT-CUT-B) fill 100vw on mobile instead of 112vw so nose and tail are not clipped. Row and card titles use the D1 rule "[Model] [Variant]" (so "S-Class S 500 4MATIC", "Yukon Denali"); the row eyebrow is "MARQUE · YEAR" like the detail page. H4 uses the horizontal legibility overlay so the headline reads over the grille; the headline is capped at 9ch so it staggers over three lines as in the inspiration. `RevealWords` keeps spaces outside the word spans (inline-block trims trailing spaces).

**Screenshots:** `qa/phase-2/home-{390,1440}.png`, `ar-{390,1440}.png`, and `detail-*` viewport captures of every section in both widths, including the pinned promise stack mid-scroll.

## Phase 3 — Collection

**Built:** C1 hero (CO-HERO-BG, COLLECTION over the silhouettes) with the filter chips on its bottom edge, sticky under the nav pill with the live "{count} cars" label. C2: the eight rows from `cars.json → collectionRows` rendered by treatment: SceneRow for Cullinan (warm, car end-side), Flying Spur Mulliner (the light room, car start-side, ink type, black serif word, light glass and badge, salon fade) and Continental GT Azure (cool, car end-side); PhotographRows for Corvette (copy end), Range Rover (copy start) and 911 (copy end); CardPairs for S-Class + Yukon and Urus + G 63. Every row carries the liquid badge and the accent-dot environment caption; Scene rows enter when they reach the viewport. Filter: single-select; leaving rows fade and shrink to .98 over 320 ms, the remaining rows FLIP into place over 800 ms, entering rows fade in, a single surviving card of a pair renders full width, ScrollTrigger refreshes, and the page scrolls to the results if the visitor filtered from deep in the list; instant under reduced motion. C3 Not here on SV-01 with a centred glass panel whose button opens WhatsApp with the sourcing service pre-filled.

**Left:** car detail pages (Phase 4), the other page bodies (Phase 5).

**Decisions:** Scene rows alternate the car side (end / start / end) so the page zig-zags with the photograph rows; the spec sketch draws all three the same way. Filter chips use the carbon-based glass of the nav pill rather than the plain frosted recipe so they stay legible over the pale Salon row. On mobile the Scene centres the word-car-copy group vertically inside the 88svh row (rows with a landscape cut-out would otherwise leave a void); the Home hero is unaffected because it has no slack. The hero → rows join uses the hero's 120 px fade with the chip bar overlapping by 68 px.

**Screenshots:** `qa/phase-3/collection-{390,1440}.png`, `ar-collection-{390,1440}.png`, `detail-*` for every treatment, the light room in both locales and widths, Not here, and the Sports / Saloon filter states.

## Phase 4 — Car detail

**Built:** `/collection/[slug]` for ten cars × two locales (20 static routes). D1 hero: the Photograph full-bleed at 100vh, liquid badge and accent caption, eyebrow MARQUE · YEAR, H1 [Model] [Variant], the colour line from `common.colourLine`, whisper, Enquire → `#enquire` and Message the concierge → WhatsApp with the car; the Flying Spur keeps the layout on the salon in ink, and the transparent nav sets in ink over a light hero. D2 glass strip of eight chips overlapping the hero by 48 px, scroll-snap under 1024 px. D3 In brief with DET-01 end-side. D4 gallery: HERO-D (2 columns), HERO-M (2 rows), DET-03 in a 3-column masonry; 86vw scroll-snap tiles with a hairline progress bar on mobile; obsidian lightbox with swipe, arrow keys, Escape, scroll lock, focus return. D5 Inside on DET-02 at 80vh. D6 Provenance on CD-PROV-01 with glass chips for the papers that apply and the report link to WhatsApp. D7 Enquire: CD-KEY-01 sticky start-side, the form end-side; on mobile the photograph is blurred behind a glass panel; title by availability. The form (§3.13): graphite 56 px fields, floating labels, +966 prefilled, native datetime, zod validation on the server action, one-sentence errors in brick, honeypot, success replaces the form with CT-DOOR-01 and the two-line confirmation; Resend when `RESEND_API_KEY` and `CONCIERGE_EMAIL` exist, otherwise the enquiry is logged and the success state still shows. D8 Also in residence: two cards, same category first. JSON-LD `Car` per page; canonical and hreflang pairs; OG image from HERO-D. The sticky bar pre-fills the car (`#enquire`, or "Ask about similar" → WhatsApp when sold). Tested both submissions in EN and AR (`form-errors-*`, `form-success-*`). Reserved and Sold verified by flipping CGT and C8 in a local copy of `cars.json` (restored, not committed): dimmed photograph, badge dots, "Ask about similar", the D7 titles, JSON-LD availability.

**Left:** Showroom, Services, Visit bodies (Phase 5); Arabic pass (Phase 6); performance, SEO, favicons, OG (Phase 7).

**Decisions:** D5 caption "{interior}, {material}, {feature}." renders the unknown fields bracketed ("Arctic White, [material], [feature].") because `cars.json` has no material or feature yet (`qa/QUESTIONS.md`). Provenance shows "Inspection report available" for every car (all ten have full histories; no per-car flag exists). Gallery real photographs (RT-CAR) are not in the manifest, so each gallery has three tiles. The lightbox has no prev/next buttons (the copy has no labels for them): swipe, arrow keys and the counter carry navigation.

**Screenshots:** `qa/phase-4/collection-rolls-royce-cullinan-{390,1440}.png`, `collection-bentley-flying-spur-mulliner-*`, `ar-collection-rolls-royce-cullinan-*`, `detail-*` per section, `form-*`, `state-*` for reserved and sold.

## Phase 5 — Showroom, Services, Visit

**Built:** Showroom S2 story (SH-04 at 4:3, three paragraphs), S3 numbers on SH-08 (four figures in Display L at the lower-start, counting up over 1200 ms on intersection, the licence static; Arabic uses the whole-word forms "طابقان" / "عنوان واحد"), S4 the space (SH-03 wide, SH-07 tall spanning two rows, SH-05 wide, one-word captions; the -M variants stacked at 4:5 on mobile), S5 coffee first (SH-09 end-side), S6 the marquee, S7 the viewing CTA on SH-06 with "Come after dark.". Services V2–V7: six 80vh photographs with one 480 px frosted card each on the dark side, alternating start / end, icon, number and name, title, line and the concierge link that opens WhatsApp with the service pre-filled; on mobile the card overlaps the photograph's lower half. V8 the viewing CTA. Visit M2: HM-MAP behind, a map tile start-side with the emblem as the pin (links to directions), the glass details panel end-side with address, hours, phone, email, parking note, directions and WhatsApp; M3 the form on the BR-03 texture with SH-09 beside it (a small tile above on mobile), the Car of interest select listing the ten cars, the aside with the concierge link; M4 the hospitality line over HM-VIEW. AutoDealer JSON-LD on every page. Visit form submission tested (`qa/phase-5/form-success-1440.png`).

**Left:** the Arabic pass (Phase 6) and performance, SEO, favicons, OG, the report (Phase 7).

**Decisions:** no live map: the address is still bracketed and no Mapbox/Google key exists, so the tile shows the HM-MAP photograph with the emblem pin and links to `settings.mapsUrl`; a dark-styled embed can replace the tile later. The Services rows alternate start / end in order (01 start … 06 end) since the spec does not assign sides per service. The showroom numbers stack in one column under 768 px so "8 marques" never breaks across lines.

**Screenshots:** `qa/phase-5/{showroom,services,visit}-{390,1440}.png`, the `ar-*` set, `detail-*` per section in both widths, `form-*`.

## Phase 6 — Arabic pass

**Checked:** every route at `/ar` at 390 and 1440 (`scripts/rtl-audit.mjs` plus viewport captures of each section). `dir="rtl"` and `lang="ar"` on the root; headings and giant words in Amiri, body and UI in IBM Plex Sans Arabic; no element with letter-spacing or uppercase; no mirrored photograph; every arrow-up-right mirrored; WhatsApp links carry the Arabic text (viewing, per car, per service); the sticky bar keeps Concierge on the end (left) side; no horizontal overflow. Layout mirrors through logical properties: nav, menu, footer, Scene rows (car on the mirrored side, word aligned to it), photograph rows, card strips and arrows, filter chips and count, glance strip scroll-snap, gallery masonry, provenance chips, the map tile and details panel, forms with right-aligned floating labels and Arabic errors, the 404. Western numerals stay in the Latin faces inside Arabic copy ("9,800 كم", "563 hp", "+966") and Latin model names keep their order through the LTR isolate. Arabic form submission tested (`qa/phase-6/form-success-390.png`).

**Fixed:** the promise section's legibility gradient now stays on the headlamp side in RTL, so the mirrored glass cards sit over darkened photography as the spec intends.

**Decisions:** the mirrored promise composition keeps the cards on the left (over the lamp, darkened) and the headline on the right, following §2.9 (layout mirrors, photographs do not). Arabic giant words use line-height 1.05 instead of .85 so Amiri ascenders and descenders are not clipped.

**Screenshots:** `qa/phase-6/ar-*` full pages for Home, Collection, two detail pages, Showroom, Services, Visit and the 404, plus `detail-*` section captures and `form-*`.

## Phase 7 — Performance, SEO, QA

**Built:** `sitemap.xml`, `robots.txt`, canonical + hreflang on every route, OG image and Apple icon generated from the masters (`scripts/brand-assets.mjs`), `metadataBase`, Open Graph and Twitter defaults, AutoDealer JSON-LD everywhere. Performance work (all measured, see `qa/REPORT.md`): `<picture>` sources per breakpoint with media-scoped high-priority preloads for the hero; the cut-out as one `<picture>` with CSS aspect ratios; the Scene entrance in CSS keyframes from first paint with GSAP at idle; image manifest and next-intl runtime removed from the client bundle; observer-driven reveals; batched headline measuring; deferred below-fold photographs with noscript copies; font preloads limited to Bodoni; duplicate srcset candidates removed; HTML halved. CLS fixed at its three sources (the mobile cut-out shrinking before load, the two-line giant word swapping fonts, the idle curtain). QA tooling: `cls-probe`, `lcp-probe`, `stage-probe`, `motion-check`, `rtl-audit`, `form-test`. Lighthouse mobile: accessibility 100, best practices 100, SEO 100 on every page; performance Home 84–86, Collection 87–89, detail 93, Showroom 95, Visit 86, Arabic Home 81. Reduced motion verified; final full-page captures for every route in both locales are in `qa/phase-7`.

**Left:** the simulated LCP target (2.5 s) and the 180 KB JS budget are not met in Lighthouse's slow-4G model (reasons and options in `qa/REPORT.md`); analytics (docs/02 §13.4); the optional desktop cursor (§3.17); client-supplied assets and copy listed in the report.

**Decisions:** the hero car paints at 55% opacity from the first frame and completes its fade in the entrance, so the LCP element is visible without waiting for JavaScript (the spec's fade-in is kept, from a lighter start). The hero entrance starts at first paint rather than at the curtain lift, which is what H0 asks for ("already mid-motion"). Manrope and Bodoni Italic are not preloaded (the spec preloads the two display faces; the italic serves only the statement below the fold). Below-fold photographs load only within 600 px of the viewport; clients without JavaScript get a single-candidate `<noscript>` image. CSS inlining was tried and reverted: no measurable gain, and it doubled the HTML.

## Phase 8 — Fonts, icons, headers

**Built (already in the working tree when the design pass began; verified building and rendering, and committed with it):** the four display and UI faces subset by `scripts/subset-fonts.py` from the untouched Google builds in `src/fonts/masters` and served from `/fonts` as immutable static WOFF2 with a `?v` token, preloaded per locale; the Lucide runtime replaced by the inlined paths in `src/components/ui/icon-paths.ts` behind one `<Icon>`; `alternateLinks: false` on the next-intl routing so the canonical and hreflang set are declared once in the head rather than repeated as a host-derived `Link:` header.

**Screenshots:** `qa/phase-8/`.

## Phase 9 — Design pass

**Checked:** every route at 1440 and 390 in both locales, section by section (`scripts/sections.mjs` walks each `<section>` and each collection row and captures the viewport at every stop), plus the moments a still cannot show (`scripts/moments.mjs`: the curtain, the hero entrance at five timestamps, the hero parallax at three depths, the pinned promise stack at four points, the filter FLIP, the mobile menu, the lightbox, hover states, reduced motion and the desktop cursor). Console clean on every route (the one warning left is a font preload on `/nothing-here`, which returns 404 before the faces are applied).

**Fixed:**
- **The grey box behind every Scene car.** `SceneCut` passed `placeholder: "blur"` through `getImageProps`, which puts the placeholder on the `<img>` as a background. Behind a transparent cut-out that background never clears (there is no `onLoad` on a raw `<img>`), so the hero, the featured exhibit and all three Scene rows carried a hard-edged grey rectangle around the car. Removed; the cars now sit on their backgrounds with only the specified drop-shadow.
- **The mobile giant word was right-aligned.** `GiantWord` emitted the desktop `text-end` at every width, so `MAY BACH`, `CULLINAN`, `MULLINER` and `AZURE` hugged the end edge on the phone instead of centring as §3.9's mobile composition draws them. `alignMobile` now centres them under 768 px and the desktop alignment is applied from `md:` up.
- **Legibility on lit photographs (§2.10).** Added one primitive — `.scrim`, a feathered band (and `.scrim--center`, an ellipse) that darkens only the copy block, full-bleed horizontally so it reads as light falling off rather than as a panel — and applied it to the statement (H2), the private-viewing CTA (H8, and the same section on Services and the Showroom), the promise headline (H4), every page-hero copy block (Collection, Showroom, Services, Visit), the showroom numbers (S3), the hospitality line (M4) and the centred exhibit's copy (H7). The `overlay-y` ramp now starts at 22% instead of 30% so bottom copy sits inside it. The nav gets a matching top fall-off while it is transparent over a hero, which is what the Showroom façade and the Salon needed.
- **The sticky filter bar collided with every row.** The chips and the "10 cars" count interleaved with each row's own liquid badge and environment caption as the row passed under them. The bar now grows a ground once it leaves the hero (an `IntersectionObserver` on a sentinel, so the hero keeps its clean bottom edge), and on desktop a collection row's badges sit at 136 px so they come to rest under the bar rather than against it.
- **Glass over bodywork.** `.glass--ground` gives a panel the nav pill's carbon ground where it has to sit over a car rather than over its environment — the hero's Now-showing card and the spec cards on Photograph and Scene rows, whose `backdrop-filter` had chrome and lamps to average and lost its type.
- **Mobile sticky-bar clearance.** The collection rows' View / Enquire pair, the Inside caption and the showroom numbers came to rest behind the 64 px bar; every bottom-anchored block on a full-height photograph now clears it.
- **Texture and ornament.** The footer's `BR-03` beam dropped from 40% to 18% and the menu's from 50% to 22% (both cut across the type); the marquee's 3% diamond watermark moved behind the tile band so it stops crossing the headline; the preloader glow is masked to a radial so its square tile no longer shows an edge on the curtain.
- **The light room on the phone.** The Flying Spur detail hero puts its ink copy where the composed photograph puts the car; the salon overlay now comes up over the car's lower half far enough for ink to read on it, so the car dissolves into the room instead of swallowing the headline.
- **Measure.** `.measure` was `52ch`, which is about 70 real characters in Manrope; at `40ch` a Body L line lands near the 52 the scale asks for. The "Not here" panel widened to 680 px so its headline keeps to two lines.

**Built:** the desktop cursor (§3.17, listed as optional and previously left): a 10 px dot that inverts its backdrop — done as `backdrop-filter: invert(1)` so it survives the cursor's own stacking context and stays visible on the light room — growing to a 72 px glass circle with *View* over a collection row, hidden over controls, absent on touch, and only after a real mouse has moved so a stationary pointer is never left invisible.

**Left:** the marque logos (client-supplied SVGs; the tiles show the names at 70% until then), the live map (needs the address and a key), and the optional hero slides 2–3.

**Decisions:** the §2.5 floor reflection is not built — every Scene background already carries the car's reflection (wet courtyard, polished salon, wet coast road), so a coded second reflection would read as a doubling. The `.glass--ground` variant is a deliberate departure from the single glass recipe, taken only where the spec's own composition puts a panel on top of a car; the panels over photography keep the plain recipe. The sticky bar's ground is near-opaque because anything less lets a row's badge show through it as a smudge; on the Salon row it reads as the same dark chrome as the nav pill above it.

**Tooling:** `scripts/sections.mjs` (walk a route section by section, and row by row on the Collection), `scripts/moments.mjs` (the motion states above), `scripts/crop.mjs` (read a detail out of a capture). `next.config.ts` now honours `MB_DIST_DIR`, so a QA build can be made and served from its own directory (`MB_DIST_DIR=.next-qa npm run build && MB_DIST_DIR=.next-qa npx next start -p 3100`) without a running `npm run dev` overwriting the build the screenshots are taken from — which is what silently unstyled two earlier capture runs.

**Screenshots:** `qa/design-pass/<route>-{1440,390}/` — every section of every route in both locales — and `qa/design-pass/moments/` for the motion states.

## Design fixes — hero scale, mobile card stack, marque logos, Scene backgrounds

**Built:**
- **The marque logos, properly.** The eight official SVGs in `public/logos` were never wired up: four of the filenames carry a suffix (`mercedes-benz-1.svg`, `range-rover-1.svg`, `chevrolet-10.svg`, `porsche-3.svg`) that `src/lib/marques.ts` was not matching, so half the strip showed names instead of marks — and the four that did resolve rendered as solid white blobs, because `filter: brightness(0) invert(1)` flattens artwork that is full-colour crests (a gold Lamborghini shield, a silver Rolls-Royce plate, a 3D Chevrolet bowtie) and flat black-on-white line art with an opaque white background rectangle (Mercedes, Porsche, GMC, Range Rover). GMC rendered as a plain white square: its background rect, inverted. New `scripts/brand-logos.mjs` derives a white-on-transparent PNG per marque into `public/logos/mono`, taking the ink from the artwork's *darkness* where the mark is line art or a detailed badge (so the Porsche crest keeps its stripes and the Rolls-Royce plate its monogram) and from the silhouette where the mark reads as one shape (the Chevrolet bowtie, whose darkness is only a bevel). Each is then trimmed and scaled so `sqrt(ink area)` is constant before a width and height cap applies — box-fitting alone makes the GMC block shout and the Mercedes star whisper — and centred on one 512×512 canvas, so the tile needs no per-logo CSS. Eight marks, ~50 KB total. `docs/02 §3.7`'s name fallback stays for a marque with no file.
- **The mobile card stack on "More than a showroom" (H4).** The pinned, scrubbed stack was desktop-only; mobile got staggered reveals. It now runs on both. A phone cannot hold the headline block and three cards in one viewport, so on mobile the head lifts and fades as the first card arrives and the card list is anchored to the foot of the pinned frame, clear of the sticky bar — the promise is read, then the proof stacks over it. `ScrollTrigger.config({ ignoreMobileResize: true })` so an address-bar resize does not jump the pin. Reduced motion and no-JS keep the plain flow column: the layout is scoped to `.promise.is-pinned`, a class `PromiseStack` only adds when it is actually building the timeline.
- **Scene background audit → `maybach-scene-background-prompts.md`.** Of the five Scenes, the Home hero and the Flying Spur row were already built as studios; the Cullinan row (a Najdi courtyard) and the Continental GT (a coast road, used by both the Home featured exhibit and Collection row 7) are photographs of real places, with detail exactly where the giant word and the copy sit. Four replacements are needed (two rooms × D + M), plus two optional. Prompts, style direction, sizes and drop-in instructions are in that file. No image in `public/images` has been touched.

**Fixed:**
- **The Scene car on mobile was too large.** `docs/02 §3.9` sketches the front-on cut-out at `112vw`; at 390 px that is a 437 px car over a 390 px frame — it bled off both edges, took two thirds of the viewport height, buried the giant word and pushed the hero's buttons past the fold. Now `96vw`, and the roofline rises over `0.18` of the giant word's box rather than `0.34`, so the car also sits ~32 px lower and more of the word survives. `Scene` takes `carWidthMobile` and `wordHiddenMobile` for a per-scene override. The Home hero is 1089 → 1056 px tall and both CTAs now land above the fold; the Cullinan row gains the same air. Desktop is untouched.

**Disagreement with the spec, recorded per `CLAUDE.md §5`:** the `112vw` figure in `docs/02 §3.9` is now `96vw`, at the client's direction. The sketch's intent — car centred, word behind it, copy below for the thumb — is unchanged.

**Checked:** `npm run build` clean, typecheck clean, lint unchanged (6 pre-existing warnings, none in touched files). Console clean on `/`, `/collection`, `/ar`, `/ar/collection` at 390 and 1440. CLS 0 on the home page at 390. No document overflow at 390 (the cut-out no longer contributes). Reduced motion: no pin, flow layout, section 953 px as before. Arabic mobile stack verified — the taller cards still clear the sticky bar.

**Left:** the four Scene backgrounds themselves, once generated from the prompts (then `npm run blur` and re-shoot). The environment captions decision in §6 of that file.

**Tooling:** `scripts/brand-logos.mjs` (rerun after adding or replacing a file in `public/logos`; commit the output), `scripts/pin-frames.mjs` (stills at chosen points along a pinned, scrubbed section's scroll span).

**Screenshots:** `qa/design-fixes/`.
