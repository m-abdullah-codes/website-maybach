/**
 * docs/02 §2.8: film grain at 5% on every photographic section for cohesion. One fixed layer over the
 * page, SVG turbulence rendered once and tiled as a background image, mix-blend-mode overlay.
 */
export function Grain() {
  return <div className="grain" aria-hidden="true" />;
}
